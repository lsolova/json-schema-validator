{
  "name": "@lsolova/json-schema-validator-worker",
  "version": "0.0.1",
  "description": "JSON schema validator worker (with Rust WASM based on jsonschema)",
  "license": "MIT",
  "author": "Laszlo Solova",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build:wasm": "cd src/wasm && sh ./build.sh",
    "build:worker": "node build.js",
    "build": "npm run build:wasm && npm run build:worker",
    "serve": "node serve.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "0.27.2",
    "tsx": "4.21.0"
  }
}
